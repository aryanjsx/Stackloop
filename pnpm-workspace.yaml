packages:
  - "apps/*"
  - "packages/*"

# Packages permitted to run install scripts. pnpm blocks all others by default.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
